# Java 程序设计复习笔记

---

## 📚 考核内容

1.  **基本语法要素**：标识符、关键字、数据类型、运算符、表达式。
2.  **控制流程语句**：分支与循环结构、数组与字符串的应用方法。
3.  **面向对象基础**：类、对象、接口的创建和实现方法。
4.  **继承与多态**：语言继承和实现方法。
5.  **综合能力**：阅读 Java 语言源程序，编写调试结构合理、清晰易懂、层次分明的程序。

---

## 第一章：概述

### 1.1 特点

**面向对象**，平台无关，多线程，动态。

#### 面向对象的三个特征：
- **封装 (Encapsulation)**
- **继承 (Inheritance)**
- **多态 (Polymorphism)**

> **封装**：将函数（方法）和数据封装到一个类中，隐藏内部细节。

---

### 1.2 基本结构

- **组成**：Java 应用程序由 **若干个类** 所构成。这些类可以在一个源文件中，也可以分布在若干个源文件中。
- **入口**：Java 应用程序 **从主类的 `main` 方法开始执行**。

---

### 1.3 开发步骤

**实例代码：**

```java
class Man {
    // 普通类
}

public class HelloW {
    public static void main(String[] args) {
        // 主方法入口
    }
}
```

> **注意**：`public` 是访问权限修饰符。

#### 开发流程步骤：

**1. 编写源文件**
- 源文件拓展名必须为 `.java`。
- 源文件名必须与 `public` 类的类名 **完全一致**。
- 一个源文件 **至多** 有一个 `public` 类。
- Java 应用程序要有主类（包含 `main` 方法）。

```java
public static void main(String args[])
```

**2. 编译**
- **源文件(.java) --> 字节码(.class)文件**
- 使用编译器 `javac.exe` 编译源文件。
- **命令**：
  ```bash
  javac HelloW.java  // 必须带拓展名
  ```
- 结果：一个类生成一个对应的字节码文件。

**3. 执行**
- **用 Java 解释器(java.exe) 来解释执行字节码文件**
- 执行含有主方法(main)的类。
- **命令**：
  ```bash
  java HelloW  // 不能带拓展名
  ```

---

### 1.4 环境变量的配置

1.  **系统变量 `JAVA_HOME`**
    > JDK 的安装根目录。

2.  **系统环境 `Path` 值**
    > `%JAVA_HOME%\bin` (为了在任意位置使用 java/javac 命令)。

3.  **Classpath 类路径**
    > `.;%JAVA_HOME%\jre\lib\rt.jar` (注意前面的点 `.` 代表当前目录)。

---

### 1.5 注释

- **单行注释**：`//`
- **多行注释**：`/* 注释 */`
- **文档注释**：`/** 文档注释 */` (可被 javadoc 提取)

---

## 第二章：Java 的基本语法

### 2.1 标识符

**命名规定**：
- 由 **字母、下划线 `_`、美元符号 `$` 和数字** 组成，长度不受限制。
- **第一个字符不能是数字**。
- **不能是关键字**（关键字通常是小写）。
- 不能是 `true`，`false` 和 `null`（虽然它们不是关键字，但也不能用）。

---

### 2.2 数据类型

#### 2.2.1 基本数据类型（8种）

| 关键字 | 类型 | 字节 | 位数 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `boolean` | 逻辑型 | - | - | false | 只能是 `true` 或 `false` |
| `byte` | 整数型 | 1 | 8 | 0 | -128 到 127 |
| `short` | 整数型 | 2 | 16 | 0 | -32768 到 32767 |
| `int` | 整数型 | 4 | 32 | 0 | **默认整型**。约 ±21亿 |
| `long` | 整数型 | 8 | 64 | 0L | 赋值需加后缀 `L`。如 `21L` |
| `float` | 浮点型 | 4 | 32 | 0.0f | 单精度，8位有效数字，需后缀 `F` |
| `double` | 浮点型 | 8 | 64 | 0.0d | **默认浮点型**。双精度，16位有效数字 |
| `char` | 字符型 | 2 | 16 | \u0000 | Unicode 字符 |

> **引用类型**：对象（Class）、数组（Array）和接口（Interface）。它们存储的是**地址**。

#### 2.2.2 整数类型的表示 (int)
4种表示方法：
- **十进制**：`123` (默认)
- **八进制**：`077` (以 `0` 开头)
- **十六进制**：`0x3ABC` (以 `0x` 或 `0X` 开头)
- **二进制**：`0b111` (以 `0b` 或 `0B` 开头)

#### 2.2.3 字符类型 (char)
- **Unicode 标准字符集**，65536个字符。
- 用 **单引号** 括起，例如 `char ch = 'A';`。

**转义字符**：
- `\n` (换行), `\b` (退格), `\t` (水平制表)
- `\'`, `\"`, `\\`

**Unicode 转义**：
- `\u` 开头，例如 `char ch = '\u0041';` (等同于 'A')。

#### 2.2.4 浮点类型
- **float 型**：必须加后缀 `f` 或 `F`。
  > 例：`3.54F`, `2e4f` (指数表示法)
- **注意**：带小数的数据默认是 `double` 类型。

#### 2.2.5 类型转换运算（重点）

**精度从“低”到“高”排列：**
> `byte` < `short`/`char` < `int` < `long` < `float` < `double`

**转换规则**：
1.  **自动类型转换**：级别低 --> 级别高。
    ```java
    float x = 100; // int 自动转 float
    ```
2.  **强制类型转换**：级别高 --> 级别低。
    ```java
    int x = (int)4.8; // 结果为 4 (精度丢失)
    ```
3.  **特例**：int 常量赋值给 byte/short，只要不超范围，不需要强转。

---

### 2.3 运算符、表达式

#### 2.3.1 算术运算符
- 双目：`+ - * / %`
- 单目：`++ --`

**混合运算精度规则**：
- 按表达式中**最高精度**运算。
- **最低按 int 计算**：如果最高精度低于 int (如 byte, char)，统统按 int 算。
  ```java
  byte b = 1;
  // byte z = 97 + b; // ❌ 错误，(97+b) 结果是 int，不能直接赋给 byte
  ```

#### 2.3.2 关系运算符
`>` `<` `>=` `<=` `==` `!=`
- 结果是 `boolean` 型。
- **注意**：比较引用类型时，`==` 比较的是**地址**（是否是同一个对象）。

#### 2.3.3 逻辑运算符
- **短路**：`&&` (与), `||` (或)
- **非短路**：`&`, `|`
- 操作数必须是 boolean 型数据。

#### 2.3.4 instanceof
格式：`对象 instanceof 类`
- 判断对象是否是该类的实例（或子类实例）。

---

### 2.4 流程控制语句

#### 2.4.1 条件语句
- `if (表达式) { ... }`
- `switch (表达式) { case ... }`
  - switch 表达式支持类型：`byte`, `short`, `int`, `char`, `String`, `Enum`。

#### 2.4.2 循环语句
- `for (初始化; 条件; 迭代) { ... }`
- `while (条件) { ... }`
- `do { ... } while (条件);`

---

### 2.5 数组的创建与使用（重点）

#### 2.5.1 声明与分配
- **声明**：`int[] a;` 或 `int a[];`
- **分配空间** (使用 `new`)：
  ```java
  int[] a = new int[4]; 
  // 数组是引用类型，存储首地址
  // 此时 a[0] 默认为 0
  ```

#### 2.5.2 数组的引用
两个相同类型的数组如果具有**相同的引用**，它们就指向同一个实体。
```java
int[] a = {1, 2};
int[] b = a; // b 和 a 指向同一块内存
```

#### 2.5.3 length 属性
- **一维数组**：`a.length` 是元素个数。
- **二维数组**：`a.length` 是它包含的**一维数组的个数**（行数）。
  ```java
  int[][] c = {{1,2}, {3,4}, {5,6}};
  // c.length 等于 3
  ```

---

### 2.6 输入和输出

#### 1. 输入 (Scanner)
```java
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner reader = new Scanner(System.in);
        // 读取整数
        int m = reader.nextInt();
        // 读取其他类型: nextByte(), nextDouble(), nextBoolean() 等
        
        // 循环读取数组示例
        int[] a = new int[10];
        for(int i = 0; i < 10; i++) {
             a[i] = reader.nextInt();
        }
    }
}
```

#### 2. 输出
- `System.out.println()`：输出并换行
- `System.out.print()`：输出不换行
- `System.out.printf()`：格式化输出

**字符串拼接陷阱**：
```java
int q = 5;
System.out.println("m+n=" + q + 1); 
// 输出: "m+n=51" (变成了字符串拼接)
// 若要计算，需加括号: "m+n=" + (q+1)
```