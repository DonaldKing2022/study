# 引言
我们在使用服务器搭建服务时，想要访问服务大部分都是通过两种方式来访问，一是通过IP+端口访问，二是通过反代后使用域名访问。
虽然使用域名配合小黄云（CDN）使用可以达到隐藏源IP的效果，但是通过SSL证书暴露也很容易获取到源站IP。
如果源站 80/443 端口对外开放，图谋不轨者也可以直接绕过 CDN 访问。
或者说我想要为一些敏感的站点，例如各种运维面板，各种不想要对外公开的服务添加一层额外认证保护
这个时候我们可以使用Cloudflare的隧道来保护我们的资源

# 创建隧道

